name: Interop

on:
  workflow_dispatch:
  workflow_run:
    workflows: [Docker]
    types: [completed]

jobs:
  interop:
    name: quic-interop-runner
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: write
    # Only run if Docker workflow succeeded (or triggered manually).
    if: >
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Install Docker Engine 28.1+
        run: |
          sudo apt-get update
          sudo apt-get install -y ca-certificates curl
          sudo install -m 0755 -d /etc/apt/keyrings
          sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
          sudo chmod a+r /etc/apt/keyrings/docker.asc
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | sudo tee /etc/apt/sources.list.d/docker.list
          sudo apt-get update
          sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
          docker --version

      - name: Install system dependencies
        run: |
          sudo apt-get install -y tshark jq python3-pip

      - name: Clone quic-interop-runner
        run: git clone --depth 1 https://github.com/quic-interop/quic-interop-runner runner

      - name: Install Python dependencies
        run: pip3 install -r runner/requirements.txt

      - name: Add zquic to implementations
        run: |
          cd runner
          jq '. + {"zquic": {"image": "ghcr.io/${{ github.repository_owner }}/zquic:latest", "url": "https://github.com/${{ github.repository }}", "role": "server"}}' implementations_quic.json > implementations_quic.json.tmp
          mv implementations_quic.json.tmp implementations_quic.json

      - name: Pull zquic image
        run: docker pull ghcr.io/${{ github.repository_owner }}/zquic:latest

      - name: Generate test certificates
        run: |
          mkdir -p certs
          # certs.sh is in runner directory
          bash runner/certs.sh certs 1 || (
            # Fallback: generate with openssl directly
            openssl ecparam -name prime256v1 -genkey -out certs/priv.key
            openssl req -new -x509 -key certs/priv.key -out certs/cert.pem -days 365 -subj "/O=interop server/"
          )
          ls -la certs/

      - name: Run interop tests (zquic server × all clients) — FAIL FAST
        env:
          CERTS: ${{ github.workspace }}/certs
        run: |
          cd runner
          # Run handshake first (fastest sanity check)
          python3 run.py \
            --server zquic \
            --test handshake \
            --log-dir logs \
            --json results.json \
            -r zquic=ghcr.io/${{ github.repository_owner }}/zquic:latest

          # If handshake passed, run remaining tests
          python3 run.py \
            --server zquic \
            --test transfer,retry,keyupdate,v2 \
            --log-dir logs \
            --json results.json \
            -r zquic=ghcr.io/${{ github.repository_owner }}/zquic:latest

      - name: Check results
        run: |
          cd runner
          echo "=== Results ==="
          jq -r '.results | flatten | .[] | "\(.name): \(.result)"' results.json

          failures=$(jq -c '.results | flatten | .[] | select(.result == "failed")' results.json)
          if [ -n "$failures" ]; then
            echo "::error::Interop failures detected:"
            echo "$failures"
            exit 1
          fi
          echo "All interop tests passed."

      - name: Update README with results table
        if: always() && hashFiles('runner/results.json') != ''
        run: |
          python3 << 'PYEOF'
          import json, re

          try:
              with open('runner/results.json') as f:
                  data = json.load(f)

              # Handle both flat and nested results formats
              if isinstance(data, dict):
                  servers = data.get('servers', [])
                  clients = data.get('clients', [])
                  results_data = data.get('results', [])
              else:
                  print("Warning: results.json has unexpected format, skipping README update")
                  exit(0)

              if not servers or not clients or not results_data:
                  print("Warning: incomplete results data, skipping README update")
                  exit(0)

              # Collect all test names - handle various result structures
              tests_set = set()
              try:
                  for s_results in results_data:
                      if isinstance(s_results, list):
                          for c_results in s_results:
                              if isinstance(c_results, list):
                                  for r in c_results:
                                      if isinstance(r, dict) and 'name' in r:
                                          tests_set.add(r['name'])
              except (TypeError, KeyError) as e:
                  print(f"Warning: could not parse results structure: {e}, skipping table generation")
                  exit(0)

              if not tests_set:
                  print("No test results found, skipping README update")
                  exit(0)

              tests = sorted(tests_set)
              zquic_idx = servers.index('zquic') if 'zquic' in servers else None

              if zquic_idx is None:
                  print("Warning: zquic not found in servers, skipping README update")
                  exit(0)

              # Build markdown table
              header = '| Test | ' + ' | '.join(clients) + ' |'
              divider = '| --- |' + ' :---: |' * len(clients)
              rows = []
              for test in tests:
                  row = f'| {test} |'
                  for c_idx in range(len(clients)):
                      symbol = '—'
                      try:
                          if zquic_idx < len(results_data) and c_idx < len(results_data[zquic_idx]):
                              for tr in results_data[zquic_idx][c_idx]:
                                  if isinstance(tr, dict) and tr.get('name') == test:
                                      symbol = '✅' if tr.get('result') == 'succeeded' else '❌'
                                      break
                      except (IndexError, KeyError, TypeError):
                          pass
                      row += f' {symbol} |'
                  rows.append(row)

              table = '\n'.join([header, divider] + rows)

              with open('README.md') as f:
                  readme = f.read()

              new_section = f'<!-- INTEROP_START -->\n{table}\n<!-- INTEROP_END -->'
              updated = re.sub(
                  r'<!-- INTEROP_START -->.*?<!-- INTEROP_END -->',
                  new_section,
                  readme,
                  flags=re.DOTALL,
              )

              with open('README.md', 'w') as f:
                  f.write(updated)

              print(f"Updated README: {len(tests)} tests × {len(clients)} clients")
          except Exception as e:
              print(f"Error updating README: {e}")
              exit(0)
          PYEOF

      - name: Commit results
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --cached --quiet || git commit -m "Update interop results [skip ci]"
          git push

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: interop-logs
          path: runner/logs/
          retention-days: 7
